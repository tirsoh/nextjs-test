import './style.css'
import { ${componentClass}Props } from './types'

export default function ${componentClass}({}: ${componentClass}Props) {
  return (
    <span>
      ${component}
    </span>
  )
}
